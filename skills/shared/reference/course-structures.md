# แหล่งหลักสูตร — course-structure ที่มีอยู่ และวิธีเพิ่มใหม่

> ย้ายมาจาก `CLAUDE.md` — อ่านเมื่อ: จะเพิ่มวิชา/ระดับชั้นใหม่ · สงสัยว่าข้อมูลหลักสูตรเชื่อถือได้แค่ไหน
> (การ lookup ปกติใช้ `no_to_token.py` ซึ่งอ่านไฟล์เหล่านี้ให้เอง ไม่ต้องเปิดเอง)

ผู้ใช้จะวาง xlsx โครงสร้างหลักสูตร สพฐ. → ถอดเป็น `course-structure-<gradeSlug>-<subjectSlug>.md`
(รูปแบบดูที่ fixture `course-structure-p1-sci.md`): front matter
(GRADE/SUBJECT/SUBJECT_CODE/**PERIOD_MINUTES**/**LANG**) + ตาราง `| No. | หน่วย | เรื่อง |`
+ บล็อกระดับหน่วย + (option) บล็อกระดับคาบ + (option) บล็อก **PREREQ**

## ★ นิยาม OBJ · IND · COMP · OUTCOME (เจ้าของโปรเจกต์กำหนด — ห้ามตีความใหม่)

| ตัวย่อ | ระดับ | คือคอลัมน์ไหนใน xlsx | ความหมาย |
|---|---|---|---|
| `OBJ|` | **หน่วย** | E `จุดประสงค์ประจำหน่วย` | สิ่งที่ตั้งใจให้เกิดเมื่อจบทั้งหน่วย |
| `IND|` | **หน่วย** | F `ตัวชี้วัด` | รหัสตัวชี้วัดแกนกลาง เช่น `ค 1.1 ม.1/1` |
| `COMP|` | **คาบ** | I `สาระสำคัญ / จุดประสงค์ประจำคาบ` | **สิ่งที่นักเรียนได้เมื่อจบคาบนั้น** |
| `SKILLS8C|` | **คาบ** | J `ทักษะ 8C ที่เน้น` | รหัส 2 ตัวที่คาบนี้เน้น (ชื่อเต็มอยู่ใน `skills-8c.txt`) |
| `OUTCOME|` | หน่วย | — ไม่มีในหลักสูตร | ผลลัพธ์ระดับหน่วยที่ทีมเขียนเสริมเอง ใช้เป็นตัวสำรองเมื่อยังไม่มี `COMP` รายคาบ |

รูปแบบบรรทัด — ระดับหน่วยไม่มีเลขคีย์ ระดับคาบต้องมีเลข No. คั่นด้วย `|`

```
### หน่วย 1 (No.1-17) — จำนวนเต็ม
- OBJ| 1. เพื่อให้นักเรียน...
- IND| ค 1.1 ม.1/1
- OUTCOME| 1. เปรียบเทียบและเรียงลำดับ...

## ระดับคาบ — COMP ...
- COMP| 1 | บอกได้ว่าจำนวนเต็มบวก จำนวนเต็มลบ และศูนย์ คืออะไร...
- SKILLS8C| 1 | C1 | C5
- POBJ| 1 | 1            # คาบนี้รับ OBJ ข้อไหนของหน่วย (ยังไม่มีข้อมูล = ต้องถาม ห้ามเดา)
```

`no_to_token.py` คืน `comp[]` เป็นของ **คาบ** เสมอ · ถ้าหลักสูตรยังไม่มีข้อมูลรายคาบ
จะถอยไปใช้ `OUTCOME` ระดับหน่วยให้อัตโนมัติ และบอกไว้ที่คีย์ `comp_source`
(`period` = ของจริง · `unit_outcome` = ของสำรอง · `none` = ไม่มีเลย)

**บล็อกระดับคาบเป็นของสคริปต์** `webapp/scripts/export_course_md.py` คอมไพล์จากฐานข้อมูล
แก้ที่ xlsx แล้ว import + export ใหม่ **ห้ามแก้ในไฟล์ .md ด้วยมือ** เพราะรอบ export ถัดไปจะทับทิ้ง

> ฟิลด์ **LANG** ขับ logic ภาษาของ song/slides/video; **PERIOD_MINUTES** ใช้ใน slides/lesson-plan

## `PREREQ|` — หัวข้อที่ต่อยอดจากหัวข้ออื่น

```
- PREREQ| 4 <- 2, 3        # No.4 ต้องรู้ No.2 และ No.3 มาก่อน
```
วางไว้ที่ไหนก็ได้ในไฟล์ (แนะนำเป็นบล็อกของตัวเองหลังตารางหัวข้อ) รับตัวคั่น `<-` `←` `:`

**ไม่ระบุ = ไม่มี prereq** — ค่าเริ่มต้นที่ปลอดภัย ระบุเฉพาะหัวข้อที่ต่อยอดกันจริง
และระบุเฉพาะ prereq **ทางตรง** (ไม่ไล่ย้อนทั้งสาย) เพื่อไม่ให้ writer อ่านเกินจำเป็น

`no_to_token.py` ตรวจให้ตอน parse — **ผิดข้อไหน exit ≠ 0 ทันที**:
No. ที่อ้างต้องมีอยู่จริง · ห้ามอ้างตัวเอง · ต้องอ้างหัวข้อที่มาก่อน (prereq < no) · ห้ามซ้ำ

**ใช้ทำอะไร**: `/content` ส่ง **path ของ `{BASE}_srcpack.md`** ของหัวข้อ prereq ให้
`content-academic` อ่านเอง (ไม่ส่งเนื้อหา — ตามกฎทองใน CLAUDE.md) เพื่อ
(ก) ไม่สอนซ้ำ (ข) ใช้ศัพท์/สัญกรณ์ชุดเดียวกัน (ค) เปิดหัวข้อด้วยการต่อยอดจากที่เรียนแล้ว
· `content-checkwork` ตรวจข้อ 8 (ความต่อเนื่อง) จากสิ่งเดียวกัน
· C1 เป็นประตูเดียว → ผลผลิตอีก 8 ชิ้นได้ความต่อเนื่องตามผ่าน srcpack อัตโนมัติ

**หัวข้อ prereq ยังไม่ได้ผลิต** (`ready: false`, `has_c1: false`) → ข้ามไป ไม่ใช่ error
แต่ orchestrator ต้องแจ้งในรายงานว่าเขียนโดยไม่มีฐานจากหัวข้อก่อนหน้า

> ความต่อเนื่องถูกเก็บเป็น **ไฟล์** ไม่ใช่ความจำใน context → `/clear` ระหว่างหัวข้อไม่ทำให้เสียอะไร

## มีจริงแล้ว
ถอดจากสารบัญหนังสือใน BookScan — OBJ/OUTCOME อิงตัวชี้วัดแกนกลาง 2551
**ควรตรวจทานกับคู่มือครูก่อนใช้เป็นทางการ**

- **`course-structure-m1-math.md`** — ม.1 คณิต 9 หน่วย 35 หัวข้อ (ฉบับปรับปรุง 2560)
- **`course-structure-p4-english.md`** — ป.4 อังกฤษ 6 หน่วย (1 Project = 1 หน่วย) `LANG| en`
  ⚠ **หน่วย 5 เท่านั้นที่ OBJ/OUTCOME ถอดจากเนื้อหาจริง** (สแกนมาเฉพาะ Project 5)
  หน่วย 1-4, 6 เป็นร่างจากชื่อ Project — ต้องสแกนเพิ่มก่อนใช้ผลิตงานจริง
- **`course-structure-p4-sci.md`** — ป.4 วิทย์ **เล่ม 1 เท่านั้น** 3 หน่วย 5 หัวข้อ (No.1-5)
  ⚠ **No.3 (บทที่ 2 ส่วนต่าง ๆ ของพืชดอก) เท่านั้นที่ถอดจากหน้าหนังสือจริง** — No. อื่นร่างจากสารบัญ
  เล่ม 2 ยังไม่มีในคลัง BookScan → ต่อ No.6 เป็นต้นไปเมื่อได้สารบัญ
