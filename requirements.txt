# แพ็กเกจที่ pipeline ของ EDU ONE ต้องใช้ — Python 3.12
#
# ปักเวอร์ชันเป๊ะโดยตั้งใจ: ผลผลิตเป็นไฟล์เอกสาร (.docx/.pptx) ที่รูปแบบต้องเหมือนกัน
# ทุกเครื่อง ถ้าปล่อยให้เวอร์ชันต่างกัน ฟอนต์/ระยะ/ตำแหน่งจะเพี้ยนแบบหาสาเหตุยาก
#
#   pip install -r requirements.txt
#
# ★ เวอร์ชันชุดนี้คือชุดที่ทดสอบแล้วว่าใช้ได้จริง (2026-09-01)

Pillow==12.2.0
python-docx==1.2.0
PyMuPDF==1.27.2.3
latex2mathml==3.81.0
lxml==6.1.1
matplotlib==3.11.1
numpy==2.5.1
python-pptx==1.0.2
pythainlp==5.3.4
PyYAML==6.0.3

# เฉพาะ Windows — ใช้วัดจำนวนหน้า A4 จริงด้วย Word COM ใน verify_docx.py
# ไม่มีก็ยังทำงานได้ แต่ verify จะข้ามการนับหน้า (ขึ้น WARN) ต้องตรวจเอง
pywin32==312 ; sys_platform == "win32"
