# บันทึกการเปลี่ยนแปลง

## 3.2.0 — 2026-09-03

**ตัวรับงาน — กดสั่งบนเว็บแล้วเครื่องทำให้เอง (ของที่ 3.1.x ยังขาดไป)**

เว็บมีปุ่ม "เริ่มทำงานบนเครื่องของฉัน" มาตั้งแต่ต้น แต่ฝั่งเครื่องยังไม่มีใครคอยรับ
งานที่กดสั่งจึงค้างอยู่ที่ "อยู่ในคิว — รอเครื่องของคุณออนไลน์" ตลอดไป และไม่มีอะไร
บนเครื่องหรือบนเว็บบอกได้เลยว่าขาดอะไร — เพราะ "ยังไม่ได้เปิดตัวรับงาน" กับ
"เครื่องปิดอยู่" เว็บเห็นเหมือนกันหมดคือเงียบ

| ไฟล์ | ที่เพิ่ม |
|---|---|
| `scripts/runner.py` | ลูปรับงาน: ทักเว็บ → ขอคิว → รัน `claude -p` → ส่งบันทึกทุก 3 วิ → ปิดงาน · ล็อกกันเปิดซ้ำด้วยพอร์ต `127.0.0.1:47615` |
| `skills/shared/scripts/stream_reader.py` | อ่าน stream JSON ของ `claude -p` แปลเป็นบันทึกภาษาคน |
| `install.ps1` | ขั้นใหม่: เขียน `~/.eduone/runner-start.ps1` + ทางลัดใน **Startup** (ย่อหน้าต่าง) แล้วเปิดให้ทันทีโดยไม่ต้องรีสตาร์ต |
| `scripts/doctor.py` | ด่านใหม่ "ตัวรับงาน" — ต่อพอร์ตล็อกดูว่า **เปิดอยู่จริงไหม** ไม่ใช่แค่มีไฟล์ |

**ที่ตั้งใจทำแบบนี้**

- **ตัวเปิดเป็น `.ps1` แยก ไม่ฝังเลขเวอร์ชัน** — หา `bin` ของปลั๊กอินใหม่ทุกครั้งที่เปิด
  เหตุผลเดียวกับฟังก์ชัน `eduone-py` ใน profile: ฝังเวอร์ชันไว้ = อัปเดตครั้งเดียวพัง
- **เขียน `.ps1` แบบมี BOM** — PowerShell 5.1 อ่านไฟล์ที่ไม่มี BOM เป็น ANSI บรรทัดไทย
  จะเพี้ยนทั้งไฟล์ (คนละกรณีกับ profile ในข้อ 6 ที่ต้องเป็น ASCII ล้วน เพราะไปต่อท้าย
  ไฟล์ของเดิมที่เราไม่รู้ encoding)
- **`doctor.py` ต่อพอร์ตจริง ไม่ดูแค่ว่ามีไฟล์** — คำถามที่พนักงานถามคือ "กดแล้วทำไม
  ไม่มีอะไรเกิดขึ้น" ซึ่งตอบได้ด้วย "เปิดอยู่ไหม" เท่านั้น
- **ยังต้องพิมพ์ `claude` ล็อกอินเองหนึ่งครั้ง** — ตัวรับงานเรียก `claude` โหมดไร้คนเฝ้า
  ซึ่งใช้การล็อกอินที่ทำไว้แล้ว มันเปิดเบราว์เซอร์ให้ล็อกอินเองไม่ได้ ข้อความปิดท้าย
  ของตัวช่วยติดตั้งจึงบอกข้อนี้ไว้ชัด ๆ

## 3.1.1 — 2026-09-03

**รันเฝ้าดูผิดโฟลเดอร์ต้องฟ้อง ไม่ใช่ส่งรายงานเท็จไปทับของจริง**

`eduone-py watch.py m1 math 1` ที่รันจากโฟลเดอร์บ้าน (ไม่ได้ cd เข้าโฟลเดอร์งานก่อน)
เดิมขึ้นหน้าจอครบทุกบรรทัดแต่ว่างเปล่าทั้งหมด อ่านได้ว่า "งานยังไม่เริ่ม" ทั้งที่ความจริง
คือกำลังส่องผิดโฟลเดอร์ — และมันยังยิงรายงาน "0 ไฟล์" ขึ้นเว็บทับความคืบหน้าจริงทุก
ครึ่งนาที ใบสั่งจึงค้างที่ 0/7 ตลอดไปทั้งที่ไฟล์ครบแล้วในเครื่อง (เกิดจริงกับใบสั่ง #2)

| ไฟล์ | ที่แก้ |
|---|---|
| `scripts/watch.py` | ไม่ใช่โฟลเดอร์งาน (ไม่มี `CLAUDE.md` / `Output/`) = ฟ้องแล้วออก exit 2 พร้อมบอกให้ cd ก่อน |
| `scripts/report.py` | ไม่มีโฟลเดอร์คาบอยู่จริง = ไม่รายงานเลย · ด่านอยู่ใน `run()` เพราะ `watch.py` เรียกตรง ไม่ผ่าน `main()` |

**ข้อคิดที่ได้**: "ไม่เห็นอะไรเลย" กับ "ไม่รู้" เป็นคนละเรื่องกัน — ตัวรายงานที่มองไม่เห็น
ข้อมูลต้องเงียบ ไม่ใช่ยืนยันว่าไม่มี เพราะปลายทางแยกสองอย่างนี้ออกจากกันไม่ได้

## 3.1.0 — 2026-09-03

**เครื่องที่รันงานรายงานความคืบหน้าขึ้นเว็บเอง — ทีมเห็นสถานะโดยไม่ต้องถามกัน**

เดิมหน้าใบสั่งบนเว็บแสดงขั้นตอนเป็นนาฬิกาสีเทาทุกขั้นตลอดกาล เพราะไม่มีช่องทางให้
เครื่องพนักงานบอกเว็บเลยสักทาง คนวางแผนงานจึงตอบไม่ได้ว่าคาบไหนถึงไหนแล้ว และไฟล์
ต้นฉบับก็ต้องกดอัปมือทีละไฟล์ (คาบหนึ่งมีราว 15-20 ไฟล์ — ไม่มีใครทำจริง)

### ของใหม่

| ไฟล์ | ทำอะไร |
|---|---|
| `skills/shared/scripts/eduone_web.py` | อ่านค่าตั้งค่า + ยิง HTTP ไปเว็บ (urllib ล้วน ไม่ใช้ requests) |
| `scripts/report.py` | สำรวจไฟล์ในโฟลเดอร์คาบ → หาใบสั่งจาก base → ส่งสถานะ + อัปไฟล์ต้นฉบับที่เปลี่ยน |
| `hooks/hooks.json` | ยิง `report.py --auto` หลังทุก Write / Edit / Bash ของ agent |
| `watch.py --no-push` | ปกติ watch.py ส่งสถานะขึ้นเว็บให้ด้วยทุกรอบ · แฟล็กนี้ไว้ปิด |
| `install.ps1` ข้อ 7/7 | ถามที่อยู่เว็บ + โทเคนส่วนตัว แล้วเขียน `~/.eduone/config.json` |
| `doctor.py` | ด่านใหม่: ตั้งค่าเว็บแล้วหรือยัง และโทเคนยังใช้ได้ไหม |

### ที่ตั้งใจออกแบบแบบนี้

- **รายงาน "ไฟล์" ไม่ใช่ "ขั้นตอน"** — agent รายงานตัวเองได้แค่สิ่งที่มันคิดว่าทำแล้ว
  ซึ่งเชื่อไม่ได้เวลามันพลาด ไฟล์ในดิสก์เป็นหลักฐานที่ตรวจซ้ำได้
- **การแปลงไฟล์เป็นขั้นตอนอยู่ฝั่งเว็บที่เดียว** (`catalog._STEP_FILES`) ฝั่งเครื่อง
  ส่งแค่รายชื่อไฟล์ ไม่ต้องรู้ว่าขั้นไหนคืออะไร — ตารางขั้นตอนเปลี่ยนได้โดยไม่ต้องออก
  ปลั๊กอินรุ่นใหม่
- **ล้มแล้วต้องเงียบ** — เน็ตหลุด เว็บล่ม โทเคนหมดอายุ เป็นเรื่องปกติ และไม่ใช่เหตุผล
  ที่จะทำให้ pipeline ผลิตสื่อหยุดกลางคัน · `report.py` จบที่ exit 0 เสมอ
- **เว้นช่วง 30 วินาที** — hook ยิงทุกครั้งที่ agent เรียก tool ซึ่งถี่มาก ตัวสคริปต์
  จำเวลาที่ส่งล่าสุดไว้ที่ `.eduone-runs/<BASE>.push.json` แล้วข้ามรอบที่เร็วเกินไปเอง
- **อัปเฉพาะไฟล์ที่เปลี่ยน** — จำ mtime ของสิ่งที่ส่งไปแล้ว ไม่ยิงไฟล์เดิมซ้ำทุกรอบ
- **โทเคนเป็นของคน ไม่ใช่ของเครื่อง** — ออกเองที่ `/me/tokens` เก็บที่ `~/.eduone/`
  นอกโฟลเดอร์งาน จะได้ไม่หลุดขึ้น git ของงานโดยไม่ตั้งใจ

ต้องใช้คู่กับเว็บที่มี `POST /api/jobs/{id}/progress` แล้ว (EDUONE-MCP-RAG `805dcfb`
ขึ้นไป) — ถ้าเว็บยังเก่า `report.py` จะเงียบแล้วข้ามไป ไม่มีอะไรพัง

## 3.0.5 — 2026-09-03

**ตั้ง policy เสร็จแล้วหน้าต่างเดิมยังใช้ไม่ได้ — ตัวช่วยโหลด profile ซ้ำให้เลย**

3.0.4 ตั้ง `ExecutionPolicy` ให้ถูกแล้ว แต่ **หน้าต่างที่รันตัวช่วยอยู่ยังพิมพ์
`eduone-py` ไม่ได้** เพราะ PowerShell อ่าน profile ตอนเปิดหน้าต่างเท่านั้น —
ตอนหน้าต่างนั้นเปิดขึ้นมา ฟังก์ชันยังไม่มี (หรือ policy ยังบล็อกอยู่)

อาการปลายทางเหมือนเดิมทุกประการ: พิมพ์แล้วขึ้น `not recognized` ทั้งที่ทุกอย่างถูก
คนใช้ไม่มีทางเดาได้ว่าต้องเปิดหน้าต่างใหม่

| | เดิม (3.0.4) | ใหม่ |
|---|---|---|
| ดอตซอร์ส profile | ทำในบล็อกเขียนไฟล์ **ก่อน**จัดการ policy — ตอนนั้น policy ยังบล็อกอยู่ จึงล้มเหลวเงียบใน `catch {}` | ย้ายไปท้ายขั้น 6/6 **หลัง** policy ผ่านแล้ว |
| ข้อความปิดท้าย | บอก "ต้องปิดแล้วเปิดใหม่" ทุกครั้งแบบไม่ได้ตรวจ | **ตรวจด้วย `Get-Command` จริง** แล้วบอกตามที่เป็น — ใช้ได้เลย หรือ "ปิดหน้าต่างนี้แล้วเปิดใหม่" |

## 3.0.4 — 2026-09-03

**ตามเก็บของที่ 3.0.3 พลาด: เขียนฟังก์ชันลง profile สำเร็จ ≠ ใช้ได้**

3.0.3 เขียนฟังก์ชัน `eduone-py` ลง PowerShell profile ให้แล้วขึ้น "เพิ่มแล้ว" สีเขียว
แต่บนเครื่องผู้ดูแลกลับยังใช้ไม่ได้ — เพราะ **ค่าเริ่มต้นของ Windows คือ
`ExecutionPolicy = Restricted` ซึ่งไม่โหลด profile เลย** ไฟล์ถูกต้องทุกตัวอักษร
แต่ไม่มีใครอ่านมัน

ที่ทำให้พลาดคือ `irm | iex` **ไม่โดนกฎนี้** (รันจากสตริง ไม่ใช่จากไฟล์) ตัวติดตั้ง
จึงทำงานผ่านฉลุยและรายงานว่าสำเร็จ ทั้งที่ผลลัพธ์ปลายทางเป็นศูนย์ — พังแบบเงียบ
ชนิดที่ต้องไปเจอเอาตอนพนักงานพิมพ์คำสั่งแล้วไม่ขึ้นอะไร

| | เดิม (3.0.3) | ใหม่ |
|---|---|---|
| `install.ps1` ข้อ 6/6 | เขียน profile แล้วจบ | เขียนแล้ว**ตรวจ `ExecutionPolicy` ต่อ** ถ้าบล็อกอยู่ เสนอ `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned` ให้ (ไม่ต้องใช้สิทธิ์ผู้ดูแล) |
| ถูกบังคับด้วย Group Policy | — | แยกออกมาบอกว่าต้องให้ฝ่ายไอทีแก้ ไม่พยายามตั้งเองให้ล้มเหลว |
| `doctor.py` | บอกแค่ "ยังไม่รู้จักคำสั่ง" | แยก **"ยังไม่ได้ตั้ง"** ออกจาก **"ตั้งแล้วแต่ policy ไม่ยอมโหลด"** — สองอย่างนี้หน้าตาเหมือนกันเป๊ะแต่แก้คนละวิธี |

พบระหว่างตรวจผลของ 3.0.3 บนเครื่องจริง: profile อยู่ที่
`D:\OneDrive\เอกสาร\WindowsPowerShell\profile.ps1` (Documents ถูก redirect ไป OneDrive
และชื่อโฟลเดอร์เป็นภาษาไทย) — ส่วนนี้ 3.0.3 จัดการถูกอยู่แล้วเพราะใช้ `$PROFILE`
ไม่ได้เดา path เอง

## 3.0.3 — 2026-09-03

**คำสั่ง `eduone-py` ใช้ได้จากเทอร์มินัลของพนักงานเองแล้ว — ตัวช่วยติดตั้งตั้งให้อัตโนมัติ**

เว็บบอกให้ "เปิดเทอร์มินัลอีกหน้าต่างแล้วรัน `eduone-py watch.py <ชั้น> <วิชา> <No>`"
ระหว่างรองาน แต่ทำตามแล้วขึ้น `CommandNotFoundException` **ทุกเครื่อง ทุกครั้ง**

สาเหตุ: `eduone-py` เป็นตัวห่อที่อยู่ใน `bin/` ของปลั๊กอิน ซึ่ง **Claude Code เติมเข้า
PATH ให้เฉพาะตอนที่ตัวมันเองรันคำสั่ง** หน้าต่างที่พนักงานเปิดเองไม่เคยเห็นโฟลเดอร์นี้
และ `install.ps1` เดิมก็ไม่ได้เติมให้ (เติมแค่ `~/.local/bin` ของ Claude Code)

| | เดิม | ใหม่ |
|---|---|---|
| ตัวช่วยติดตั้ง | 5 ขั้น จบที่แพ็กเกจ Python | **6 ขั้น** — ขั้น 6/6 เขียนฟังก์ชัน `eduone-py` ลง PowerShell profile ให้ |
| `doctor.py` | ไม่รู้ว่าเทอร์มินัลของคนใช้รู้จักคำสั่งไหม | ด่านใหม่ **ถามเชลล์จริงโดยตัด `bin` ของปลั๊กอินออกจาก PATH ก่อน** ไม่งั้นผ่านหลอก ๆ ทุกครั้ง |
| README | ไม่มีขั้นนี้เลย | ขั้นที่ 5 ของการติดตั้งด้วยมือ ทั้ง Windows และ macOS/Linux |

### รายละเอียดที่ตั้งใจทำแบบนี้

- **ฟังก์ชันหาเวอร์ชันตอนเรียก ไม่ฝังเลขเวอร์ชัน** — path มีเลขเวอร์ชันอยู่
  (`…/edu-one/3.0.3/bin`) เอาไปใส่ PATH ตรง ๆ พออัปเดตปลั๊กอินครั้งเดียวก็พัง
- **เขียน profile แบบ AllHosts และเขียนให้ทั้งสอง edition** ถ้าเครื่องมีทั้ง 5.1 และ 7 —
  คนละ edition คนละไฟล์ profile ตั้งให้ตัวเดียวแล้วไปเปิดอีกตัวจะงงว่าทำไมหาย
- **เนื้อฟังก์ชันเป็น ASCII ล้วน** — profile ของพนักงานอาจถูกเขียนด้วย encoding อื่นมาก่อน
  ต่อท้ายด้วยข้อความไทยจะได้ BOM กลางไฟล์แล้วบรรทัดไทยเดิมเพี้ยนทั้งไฟล์
- **ต่อท้าย ไม่เขียนทับ** และข้ามถ้ามีอยู่แล้ว — รันตัวช่วยซ้ำกี่รอบก็ได้ฟังก์ชันเดียว

### แถมที่แก้ไปด้วยเพราะเป็นบั๊กเดียวกัน

- `Get-PluginDir` ใน `install.ps1` เรียงด้วย `Sort-Object Name` ซึ่งตัดสินว่า
  **3.0.9 ใหม่กว่า 3.0.10** — เปลี่ยนเป็นเรียงตามเวอร์ชันจริง
- คำสั่ง pip ในหัวข้อติดตั้งด้วยมือของ README พังอยู่ — `"$p\requirements.txt"`
  กลายเป็น `"$p` ขึ้นบรรทัดใหม่ `equirements.txt"` (`\r` โดนตีความเป็นขึ้นบรรทัด)
  ใครคัดลอกไปใช้จะรันไม่ได้เลย

## 3.0.2 — 2026-09-02

**ลดของที่ต้องเขียนและลดบริบทที่ต้องอ่าน ตามที่เจ้าของโปรเจกต์แจ้งหลังตรวจผลผลิตจริงของ 3.0.1**
หลักการเดียวกันทั้งชุด: กฎที่เครื่องนับได้ให้ย้ายเข้า `validate_spec` และของหนักให้ส่งเป็น path

### สไลด์

| # | เดิม | ใหม่ |
|---|---|---|
| 1 | `speaker_note` ใส่เวลารายหน้า และรวมต้องได้ 50 นาที | **ห้ามมีเวลาใน `speaker_note` เลย** (FAIL) — สไลด์ไม่ใช่ตัวคุมเวลาคาบ ครูตัดหน้าที่ไม่ใช้เองอยู่แล้ว |
| 2 | prompt เขียนว่า "ตั้งเป้า ≤ 36 หน้า" | **~20 หน้า · WARN > 22 · FAIL > 30** และย้ายเข้าประตู ไม่ใช่แค่คำแนะนำ |
| 3 | ไม่มีกฎเรื่องการยกประโยค | **ยกประโยคจากต้นทางมา ห้ามเรียบเรียงใหม่** + ประตูเทียบตัวเลขบนสไลด์กับต้นทางผ่าน `--ref` |

กฎข้อ 1 ไม่ได้แค่เปลืองที่เขียน — สไลด์ C1 ของ `M1-Math_U1_1_2` ถูกตีกลับเพราะโน้ตรวมได้
69 นาที ต้องยุบ 2 หน้าแล้ว **เปลี่ยนชื่อไฟล์ PNG ทั้ง 6 ไฟล์** เพราะเลขหน้าขยับ

กฎข้อ 3 มาจากการไล่เทียบข้อผิดพลาดที่หลุดถึง checkwork ทั้งสามข้อในรอบเดียวกัน —
**ทุกข้อเกิดจากการเรียบเรียงใหม่ทั้งที่ต้นทางเขียนถูกอยู่แล้ว** เช่นต้นทางเขียน
"ใครควรยืนทางขวา" สไลด์เขียน "ใครต้องไปยืนทางขวาของศูนย์" ซึ่งเป็นเท็จ
($-1$ กับ $-8$ อยู่ทางซ้ายของศูนย์ทั้งคู่)

### แผนการสอน

- **ขั้นวัดผลการเรียนรู้เป็นข้อความสำเร็จรูปบรรทัดเดียว** writer ไม่ต้องเขียนเอง
  ประตูตรวจแบบ verbatim และห้ามมีข้อย่อย
- **เพดานความละเอียด**: ไม่เกิน 6 ข้อต่อขั้น · ข้อละไม่เกิน 220 อักษร
  แทนการให้ checkwork ตัดสินว่า "ทำทันไหม" ซึ่งรอบ `M1-Math_U1_1_2` กินไป 3 รอบ
  (วัดจริงก่อนตั้งเพดาน: ขั้นสอน 6-9 ข้อ ยาวสุด 437 อักษร)
- **ลำดับข้อต้องต่อเนื่อง 1..n และห้ามมีบรรทัดซ้ำ** — ประตูเดิมตรวจแค่ชื่อขั้นกับผลรวมนาที
  จึงปล่อยไฟล์ที่ขั้นสอนกลายเป็น `5) 6) 3) 8) 5) 6) 7) 8) 9)` ผ่านแบบ exit 0 (เกิดจริง)
  ผลคือ COMP ครึ่งหนึ่งไม่มีกิจกรรมรองรับ โดยไม่มีสัญญาณเตือนใด ๆ

### เนื้อหา

- **C2 ต้องไม่ยาวกว่า C1** — ยาวกว่า = WARN · เกิน +10% = FAIL
  เดิมกติกาเขียนแค่ "ความรู้ต้องครบเท่า C1" writer เลยตีความว่าต้องยาวไม่แพ้กัน
  (`M1-Math_U1_1_2`: C1 10,788 อักษร · C2 12,502 = +16%) แล้วสไลด์ C2 ที่ derive ต่อก็บวมตาม

### ผลของ checkwork ส่งเป็น path ไม่ส่งเนื้อหา

`paths.py` เพิ่มคีย์ **`review_md`** (`{BASE}_gate_review.md` · หลายไฟล์เติม tag เช่น
`{BASE}_gate_L1_review.md`) — checkwork เขียนรายการแก้ลงไฟล์ แล้วคืนมาแค่ `VERDICT`
กับสรุปหนึ่งบรรทัด orchestrator ส่ง path ต่อให้ writer อ่านเอง

เดิม orchestrator ต้องพิมพ์รายการแก้ยาว ๆ ลง prompt ทุกครั้งที่ส่งกลับให้ writer
(รอบ `M1-Math_U1_1_2` เกิดขึ้น 7 ครั้ง) ซึ่งค้างในบริบทของแม่ตลอด pipeline และเป็นการ
คัดลอกด้วยมือที่ตกหล่นเมื่อไร writer ก็แก้ไม่ครบตามนั้น — เป็นกฎ "ส่ง path ไม่ส่งเนื้อหา"
ที่ยังไม่เคยใช้กับผลของ checkwork

### ไม่ได้แก้ (ตั้งใจ)

`image_prompt` ยังอยู่ครบทุกหน้าตามเดิม — พนักงานสั่งทำสไลด์แยกจากเนื้อหาได้
ทีมที่กดทำเฉพาะสไลด์ต้องได้ใบสั่งผลิตรูปติดไปด้วย

### ผลกระทบกับ artifact เดิม

artifact ที่ผลิตก่อนหน้านี้จะไม่ผ่านประตูใหม่ (สไลด์เกือบทุกชุดมีเวลาใน `speaker_note` ·
แผนทุกไฟล์เกินเพดานความละเอียด) — **ไม่ต้องย้อนกลับไปแก้** เป็นชุดทดสอบ
ประตูใหม่มีผลกับการผลิตรอบถัดไป


## 3.0.1 — 2026-09-02

**แก้แผนการสอน 5 จุดตามที่เจ้าของโปรเจกต์แจ้ง** หลังตรวจผลผลิตจริงของ 3.0.0

| # | เดิม | ใหม่ |
|---|---|---|
| 1 | หัวข้อ 1 เป็นชื่อเอกสาร "แผนการจัดการเรียนรู้ที่ 1 เรื่อง ... (การเรียนรู้เชิงสำรวจ 50 นาที)" ยาว 277 อักษร | **ชื่อกิจกรรม** สั้น จำง่าย เช่น "เดินทางไปกับเส้นจำนวน" (เพดาน 60 อักษร) |
| 2 | ขั้นวัดผลแต่ง "ใบงานท้ายคาบ 6 ข้อ" ขึ้นเอง | **ให้นักเรียนทำแบบฝึกหัดของคาบนี้** (ชุด 30 ข้อที่ผลิตคู่กัน ครูเลือกมา ~10 ข้อ) |
| 3 | หัวข้อ 5 ยัดหลายรายการไว้บรรทัดเดียว | **1 รายการ = 1 bullet** |
| 4 | หัวข้อ 6 ระบุ "ใบงานข้อ 1" | บอกแค่ **วิธีวัด / เครื่องมือ / เกณฑ์ผ่าน** ครูเลือกข้อเอง |
| 5 | Rubric เป็น **4C** | Rubric วัดจาก **มิติของ 8C** |

### Rubric 8C — `4c_standard` ใช้ไม่ได้แล้ว

`RUBRIC_4C_STANDARD` ถูกแทนด้วย `RUBRIC_8C` ครบ C1–C8 และใช้ **เฉพาะสองข้อที่หลักสูตร
ระบุว่าคาบนั้นเน้น** (บรรทัด `SKILLS8C|`) ได้ตาราง 2 มิติ × 4 ระดับ ซึ่งครูใช้จริงได้ใน
คาบเดียว — ครบ 8 ข้อจะเป็น 32 ช่องซึ่งวัดไม่ไหว และคำอธิบายครบ 8 ข้ออยู่ในหัวข้อ 3 แล้ว

```json
["7. เกณฑ์การประเมิน (Rubric)",
 {"template": "8c_standard", "codes": ["C1", "C5"], "topic": "...", "skill": "..."}]
```

`codes` มาจากหลักสูตร ไม่ใช่สิ่งที่ writer เลือก · `validate_spec` เรียก
`expand_rubric_template` ตั้งแต่ตอนตรวจ ไม่รอไปพังตอน build

### `--peer` — ประตูตรวจข้ามไฟล์ของ L1/L2

หลังเปลี่ยนเป็น 8C พบว่า writer ใส่ `topic`/`skill` เหมือนกันเป๊ะทั้งสองแผน ตาราง Rubric
จึงออกมาตัวอักษรเดียวกันทุกบรรทัด ทั้งที่กิจกรรมต่างกันมาก

`validate_spec.py lesson_plan <json> --peer <อีกแผน>` ตรวจว่าต่างกันจริง:
ชื่อกิจกรรม · `plan_title` · `topic`/`skill` ใน Rubric — กฎกลุ่มนี้ตรวจในไฟล์เดียวไม่ได้
(`codes` ไม่ตรวจ เพราะมาจากหลักสูตร)

### ประตูอื่นที่เพิ่ม

หัวข้อ 1 ห้ามคำว่าแผนการจัดการเรียนรู้/แบบที่ 1/Inquiry-Based · ขั้นวัดผลต้องอ้าง
แบบฝึกหัดของคาบ (ตัดหัวบรรทัดออกก่อนตรวจ ไม่งั้นคำว่า "แบบฝึกหัด" ในชื่อขั้นเองทำให้
ผ่านฟรี) · หัวข้อ 5 ต้องเป็น list · หัวข้อ 6 จับรูปแบบ "ใบงานข้อ 1"

## 3.0.0 — 2026-09-02

**เอกสารตรงหลักสูตรจริง + ลดโทเคน** — รอบนี้เป็น major เพราะ artifact เดิมใช้ต่อไม่ได้
โดยไม่ migrate (หน้าปกเปลี่ยนจำนวนแถว · แผนการสอนเปลี่ยนโครง)

### ⚠ ต้องทำเมื่ออัปเดต

artifact ที่ผลิตก่อนหน้านี้จะตกประตูตรวจทันที — ตั้งใจให้เป็นแบบนั้น เพราะของเดิม
มีข้อมูลผิดช่องอยู่จริง

```bash
export PYTHONIOENCODING=utf-8
eduone-py migrate_cover.py <gradeSlug> <subjectSlug> <No>            # ดูก่อน
eduone-py migrate_cover.py <gradeSlug> <subjectSlug> <No> --apply    # แล้ว build ใหม่
```
แผนการสอน L1/L2 ของเดิมต้อง **เขียนใหม่** ด้วย `/lesson-plan` (โครงเปลี่ยนจาก 9 เป็น 8 หัวข้อ)

### ★ COMP เคยหมายถึงคนละอย่างอยู่ 3 ที่

เจ้าของโปรเจกต์กำหนดนิยามให้แล้ว จึงสะสางทั้งท่อ

| ตัวย่อ | ระดับ | xlsx | ความหมาย |
|---|---|---|---|
| `OBJ` | หน่วย | E | จุดประสงค์ประจำหน่วย (เหมือนเดิม) |
| `IND` | หน่วย | F | **ตัวชี้วัด** — ของใหม่ เดิมถูกเรียกผิดว่า comp |
| `COMP` | **คาบ** | I | **สาระสำคัญ / จุดประสงค์ประจำคาบ** — สิ่งที่นักเรียนได้เมื่อจบคาบ |
| `OUTCOME` | หน่วย | — | ผลลัพธ์ที่ทีมเขียนเสริมเอง (คือ `COMP` เดิม เก็บไว้เป็นตัวสำรอง) |

`no_to_token.py` คืน `comp[]` เป็นของคาบโดยอัตโนมัติ — agent ที่รับ `comp[]` อยู่แล้ว
ไม่ต้องแก้อะไร · `comp_source` บอกที่มา (`period` / `unit_outcome` / `none`)

**ผลข้างเคียงที่สำคัญที่สุด**: รอบแก้ที่แพงที่สุดที่เคยเกิด (orchestrator เดาว่าคาบไหน
รับ COMP ข้อไหนแล้วเดาผิด จนต้องเขียน C1 ใหม่ทั้งฉบับ) **หายไปทั้งชุด** เพราะ COMP
เป็นของคาบโดยนิยาม ไม่มีอะไรให้เดาอีก

### เอกสาร

- **หน้าปก 6 → 7 แถว** เพิ่ม `ตัวชี้วัด` ต่อจาก `หน่วย` · สองแถวท้ายเปลี่ยนเป็น
  `จุดประสงค์ประจำหน่วย` และ `สาระสำคัญ / จุดประสงค์ประจำคาบ` — ชื่อตรงคอลัมน์ใน xlsx
  พอดี เปิดหลักสูตรเทียบหน้าปกได้ทันที
- **หัวกระดาษ** `... > หน่วยX > หัวข้อY > เรื่องZ` (ตัด `คาบn` ทิ้ง) มาจากแหล่งเดียว
- **แผนการสอน 9 → 8 หัวข้อ** เพิ่ม **ทักษะ 8C** (ครบ C1–C8 ทุกแผน ชื่อทางการอยู่ที่
  `skills-8c.txt`) และ **การนำไปใช้ในชีวิตประจำวัน** · ตัดสาระสำคัญ/สาระการเรียนรู้
  (ย้ายขึ้นหน้าปก) · `สื่อ/แหล่งเรียนรู้` → `วัสดุและอุปกรณ์` ·
  `ขั้นทำแบบฝึกหัด` → `ขั้นวัดผลการเรียนรู้` · K/P/A บังคับ 3 บรรทัด ≤300 อักษร
- **C1/C2 ไม่มีหัวข้อสรุป** · **ตารางไม่บังคับ** แต่ถ้ามีต้องเป็นตารางจริง
  (header ครบ · ≥2 คอลัมน์ ≥2 แถว · เซลล์ไม่เกิน 160 อักษร)
- **แบบฝึกหัด** ตัดบรรทัด "(ตัวเลือกสีแดง = คำตอบที่ถูก)" · หน้าเฉลยเป็น
  **ตาราง 3 คอลัมน์** `ข้อ / คำตอบที่ถูก / วิธีคิด` · เขียนชัดว่าไม่มีรูปเลยก็ผ่าน
  และ checkwork **สั่งเพิ่มรูปไม่ได้**

### ★ บั๊กจริงที่พบในเอกสารที่ผลิตไปแล้ว

**ตัวหนาไหลไปลงคำอังกฤษในวงเล็บ** — ใน `M1-Math_U1_1_1_C1.docx` run ที่เป็นตัวหนาคือ
`" (counting number) "` ส่วน `จำนวนนับ` ที่ควรหนากลับไม่หนา ตัวหนาเลื่อนผิดช่องทั้งเอกสาร

เหตุ: `add_paragraph` เรียก `_split_math_spans` ก่อน `_parse_inline` แต่
`_NONTHAI_RE` นับ `**` เป็นอักขระไม่ใช่ไทย จึงตัดคู่ `**...**` ขาดจากกัน แล้วเรียก
`_parse_inline` แยกทีละท่อนโดยรีเซ็ตสถานะ bold ทุกครั้ง — `**` ที่ควรปิดกลายเป็นที่เปิด
ของท่อนถัดไป · แก้ด้วย `render_spans()` ตัวเดียวที่ทำตามลำดับถูกต้อง · **pptx มีบั๊ก
เดียวกันเป๊ะและหายไปพร้อมกัน**

**เนื้อเพลงใช้ `/n` แทนการขึ้นบรรทัด** — ทั้ง 3 วิชาที่เคยผลิต Suno จะได้ตัวอักษร
`/n` ไปร้องด้วย และไม่มีใครเห็นจนกว่าจะฟังไฟล์เสียง (`check_song` ที่เพิ่งเพิ่มจับได้ทันที)

**ประตูตรวจเวลาคาบหลุดเงียบ** — เดิมหาแถวกิจกรรมด้วย `startswith("6.")` ถ้าหาไม่เจอ
จะข้ามการตรวจไปโดยไม่มี error พอเปลี่ยนเลขหัวข้อทีเดียวประตูหายทั้งบานโดยไม่มีใครรู้
ตอนนี้ทุกแถวอ้างผ่านดัชนีที่ระบุ และหาไม่เจอ = FAIL ทันที

### นโยบายสมการ — `math_policy.py` (โมดูลใหม่ ใช้ร่วม docx + pptx)

เดิม docx บังคับให้ `$...$` ทุกอันเป็นวัตถุ Equation โดยไม่มีเกณฑ์ ขณะที่ pptx
มีเกณฑ์อยู่แล้ว นับจากไฟล์จริง: `_ex.json` 220 ช่วง · `_C1.json` 94 · `_slides_C1.json` 58
เข้าข่ายต้องใช้ Equation จริง **0 ทั้งหมด** (ยอดฮิตคือ `-2` `0` `1` `-3`)

ตอนนี้ซ้อนชั้น (เศษส่วน กรณฑ์ เมทริกซ์ ผลรวมมีขอบเขต) → Equation · นอกนั้น →
ข้อความธรรมดา + ตัวยก/ตัวห้อยจริง ซึ่งคัดลอกได้ ค้นหาเจอ แก้ในเวิร์ดได้
**ตัวตัดสินอยู่ที่ตัว render ไม่ใช่ที่ writer** — artifact เดิมไม่ต้องแก้ build ใหม่ก็ถูก

### โทเคน

- **`scripts/run_period.ps1`** — ยิง `claude -p` **แยกโปรเซสต่อคาบ** จึงได้เซสชันใหม่
  ทุกครั้งโดยไม่ต้องพึ่งวินัยคน · `/edu-one` เพิ่ม preflight หยุดเตือนถ้าเซสชันนี้
  ทำงานอื่นมาก่อน (จากการวัดจริง: แม่กิน 54% ของ $308 และเริ่มที่ 585,680 โทเคน
  ตั้งแต่ turn แรกเพราะเซสชันนั้นทำงานเว็บมาทั้งวัน)
- **`scope_card.py` → `{BASE}_scope.md`** บัตรขอบเขตคาบ — สิ่งที่ต้องส่งมอบ ·
  เรียนไปแล้ว · **ยังไม่สอนห้ามล้ำ** · PREREQ · ทุก prompt ส่งแค่ path
  (รอบก่อน orchestrator พิมพ์ข้อห้ามชุดเดียวกันซ้ำใน 11 prompt)
- **`validate_spec.py --report` → `{BASE}_gate.md`** บัตรผลตรวจของเครื่องพร้อมค่าที่วัดได้
  (โควตา 15/8/7 · การกระจายเฉลย ก8 ข8 ค7 ง7 · เวลา 50/50) checkwork อ่านแล้วไม่ต้องนับซ้ำ
- **`checkwork-contract.md`** สัญญาร่วมของทั้ง 7 ตัว + รวม verdict เป็น `VERDICT:`
  แบบเดียว (เดิม content-checkwork ใช้ `CHECKWORK: ALL-PASS` ตัวเดียว)
- **อุดรูรั่ว "ส่ง path ไม่ส่งเนื้อหา" ที่ orchestrator ลูก 4 ตัว** — หนักสุดคือ
  `lesson-plan` ที่อ่าน C1 เข้าแม่ **แล้วยังส่งข้อความ C1 ต่อเข้า prompt ของลูกอีก 2 ตัว**
  = จ่ายค่าเนื้อหาชุดเดิม 3 เท่า · `exercise` เลิกให้แม่ `Read` ภาพสแกน BookScan
- **`usage_report.py` → `{BASE}_usage.json`** เก็บโทเคนจริงทั้งการรันจาก event `result`
  ของ CLI (รวมทุก sub-agent และทุกรอบแก้) — เดิมเลขนี้หายไปกับ session ทุกครั้ง

### ประตูเชิงเครื่องที่เพิ่ม

`check_song` · `check_video` · `check_game` (เดิมไม่มีเลย LLM ต้องนั่งนับเอง) ·
`--agents` assert `model == opus` จริงตาม `CLAUDE.md` ข้อ 8 (เดิม lint แค่ว่ามีคีย์) ·
`CLAUDE.md` ของ repo ปลั๊กอินเอง (เดิมมีแต่ใน repo งาน คนที่ clone ปลั๊กอินไม่เห็นกฎเลย)

### แก้เล็ก

`run_period.ps1` ต้องเป็น UTF-8 with BOM + CRLF ไม่งั้น Windows PowerShell 5.1
อ่านเป็น ANSI แล้วอักษรไทยเพี้ยนจนพาร์สไม่ผ่าน (บังคับไว้ใน `.gitattributes` แล้ว)

## 2.2.0 — 2026-09-02

**แก้ 3 บั๊กที่เจอจากการผลิตสื่อจริง 1 คาบเต็ม** (ม.1 คณิต คาบ 1 — C1 · C2 · L1 · L2
· แบบฝึกหัด 30 ข้อ · เพลง · สไลด์ 4 ชุด) ทั้งสามตัว `smoke.py` จับไม่ได้ เพราะไม่ใช่
ปัญหาโครงสร้าง แต่โผล่ตอนใช้งานจริงเท่านั้น

### ★ `template_root()` ชี้เข้าโฟลเดอร์ปลั๊กอิน — build .pptx ตายทุกครั้ง

หาโฟลเดอร์เทมเพลตด้วยการนับถอย 4 ชั้นจากตำแหน่งไฟล์ตัวเอง ซึ่งถูกตอนสกิลยังอยู่ใน
repo งาน แต่พอย้ายเป็นปลั๊กอินแล้วถอยไปโผล่ที่

    ~/.claude/plugins/cache/eduone/edu-one/Slide Master Template   ← ไม่มีอยู่จริง

**เทมเพลตเป็นไฟล์ของผู้ใช้** (แต่ละทีมมีแบบของตัวเอง) จึงต้องอ่านจาก `WORK_ROOT`
ที่ `_root.py` แยกไว้ให้แล้ว ไม่ใช่จากโฟลเดอร์ปลั๊กอินที่ถูกทับทุกครั้งที่อัปเดต
ตั้งทับได้ด้วย `EDUONE_TEMPLATE_DIR`

นี่คือบั๊กชนิดเดียวกับที่การแยก repo ควรกวาดไปหมดแล้ว — หลุดมาตัวเดียวเพราะไม่มีใคร
รัน `/slides` เลยตั้งแต่ย้าย

### แผนการสอนยาวเกิน 5 หน้าได้ (เนื้อหายังคุม 3–5 เหมือนเดิม)

`verify_docx.py` บังคับ 3–5 หน้ากับทั้ง `content` และ `lesson_plan` แต่แผนมี 9 แถวที่ต้องมี
ทั้งจุดประสงค์ กิจกรรมรายขั้น สื่อ และเกณฑ์การวัด — **ยิ่งคุณภาพดีขึ้นยิ่งยาว**
การบีบให้อยู่ใน 5 หน้าจึงเป็นการลงโทษแผนที่ทำถูก

เกิดจริง: checkwork สั่งแก้ 9 จุด (เพิ่มเกณฑ์การวัด รายละเอียดสื่อ ภารกิจคู่ขนาน)
แผนโตจาก 5 เป็น 6 หน้า แล้วตกด่าน verify ต้องเสียอีกรอบไปตัด

ตอนนี้ `lesson_plan` เกิน 5 หน้าได้ (เหลือ WARN) · `content` ยังบังคับ 3–5 ตาม
`CLAUDE.md` ข้อ 4 · ขอบล่าง 3 หน้าคงไว้ทั้งคู่ (สั้นกว่านั้นมักแปลว่ามีแถวว่าง)

### `/edu-one` รู้แล้วว่าผลผลิตไหนถูกพักไว้

เจ้าของโปรเจกต์สั่งพักวิดีโอกับเกม และเว็บกันไม่ให้สั่งได้แล้วผ่าน
`DEFERRED_PRODUCTS` ใน `webapp/app/catalog.py` **แต่ปลั๊กอินไม่รู้เรื่องนี้เลย**
คนที่รัน `/edu-one` ตรง ๆ ไม่ผ่านเว็บจึงได้ของที่ยังไม่เปิดใช้มาด้วย (เกิดจริง
เสียไป ~238,000 โทเคน)

เพิ่มหัวข้อ "ผลผลิตที่ถูกพักไว้ — ห้ามผลิต" ใน `edu-one/SKILL.md` พร้อมหมายเหตุว่า
รายชื่อต้องตรงกับฝั่งเว็บ **แก้ที่ไหนต้องแก้อีกที่ด้วย**

## 2.1.0 — 2026-09-01

**`smoke.py` — พิสูจน์ว่าโครงยังต่อกันติด โดยไม่เรียก agent และไม่กินโควตา**

    eduone-py smoke.py

`doctor.py` ตอบว่า "เครื่องพร้อมไหม" (มี python · มีแพ็กเกจ · มีปลั๊กอิน)
ไฟล์นี้ตอบว่า **"ตรรกะยังถูกไหม"** — คำถามคนละข้อ และข้อหลังคือข้อที่พังเงียบ ๆ
ตอนเปลี่ยนโครงสร้าง อย่างตอนหลักสูตรเพิ่มชั้น "คาบ" เข้ามาใน 2.0.0

ตรวจ 6 หมวด 18 ข้อ · เทียบหลักสูตร 2 แบบทุกข้อเสมอ
  รายคาบ (m1 math)  BASE 4 ท่อน โฟลเดอร์ซ้อน 1 ชั้น
  แบบเดิม (p4 sci)  BASE 3 ท่อน — **ต้องไม่เปลี่ยนอะไรเลย**

★ ที่ต้องเทียบสองแบบทุกข้อ เพราะบั๊กส่วนใหญ่ตอนเพิ่มชั้นใหม่ ไม่ได้ทำให้ของใหม่พัง
  แต่ทำให้ **ของเดิม** พังแทน — ซึ่งไม่มีใครไปกดดูเพราะคิดว่า "ก็ของเดิม"

หมวดที่ตรวจ
  1. โฟลเดอร์ผลผลิตซ้อนถูกชั้น (คาบอยู่ใต้หัวข้อหลัก · หัวข้อหลักอยู่ใต้หน่วย)
  2. ผลผลิตระดับหน่วยไม่หลุดไปอยู่ในโฟลเดอร์หัวข้อ (บั๊กที่แก้ใน 2.0.1)
  3. ชื่อไฟล์ทุกผลผลิตขึ้นต้นด้วย BASE ที่ถูก
  4. metadata 17 คีย์ที่ writer/checkwork ต้องใช้ ครบและไม่ว่าง
  5. ประตูตรวจโควตาความยาก — ปล่อย 15/8/7 และ 10/5/5 · จับ 10/10/10
     (ตรวจว่า "จับได้" ด้วย ไม่ใช่แค่ "ปล่อยผ่าน" — ประตูที่ปล่อยทุกอย่างไม่ใช่ประตู)
  6. สร้าง .docx จริงที่ path ซ้อนลึก แล้ว verify กลับ

ทำงานในโฟลเดอร์ชั่วคราวทั้งหมด ไม่แตะ Output จริง และลบทิ้งเมื่อจบ

## 2.0.1 — 2026-09-01

**แก้ path ของผลผลิตระดับบท/หน่วยที่ชี้ผิดหลังเปลี่ยนมาเป็นรายคาบ**

`unit_paths()` หาโฟลเดอร์หน่วยด้วยการถอยขึ้นหนึ่งชั้นจากโฟลเดอร์งาน ซึ่งถูกตอน
โครงมี 3 ชั้น แต่หลักสูตรรายคาบลึกกว่าหนึ่งชั้น

    Output/M1/Math/M1-Math_U1/M1-Math_U1_4/M1-Math_U1_4_1
                              ^^^^^^^^^^^^ ถอยชั้นเดียวได้ตรงนี้ = โฟลเดอร์ "หัวข้อ"
                   ^^^^^^^^^^ ที่ถูกคือตรงนี้ = โฟลเดอร์ "หน่วย"

ผลคือข้อสอบระดับบทจะไปตกในโฟลเดอร์หัวข้อ และ**ไฟล์ระดับหน่วย 5 ชุดที่ทำไว้แล้ว
จะหายไปจากสายตา pipeline** เพราะหาไม่เจอที่เดิม

เปลี่ยนมาประกอบจากส่วนที่รู้แน่ (`grade_token` · `subject_token` · `unit_folder`)
ถูกต้องไม่ว่าโครงจะลึกกี่ชั้น · ตรวจแล้วทั้งหลักสูตรรายคาบและแบบเดิม และเทียบกับ
ไฟล์ที่มีอยู่จริงบนดิสก์แล้วตรงกัน

## 2.0.0 — 2026-09-01

**ผลิตเป็นรายคาบแล้ว ไม่ใช่รายหัวข้อ** — ขึ้นเลขหลักเพราะความหมายของ No. เปลี่ยน

ไฟล์ xlsx โครงสร้างการสอนรายคาบของ สสวท. ละเอียดกว่าตารางหัวข้อเดิมหนึ่งชั้น
ม.1 คณิตศาสตร์ที่เคยเป็น 35 หัวข้อ แตกเป็น **120 คาบ** และผู้ใช้กำหนดให้ผลิต
1 ชุดสื่อต่อ 1 คาบ

### ★ ต้องอ่านก่อนอัปเดต

**เลข No. ของวิชาที่มีหลักสูตรรายคาบเปลี่ยนความหมายทั้งหมด** — ตอนนี้ No. นับเป็น
"คาบ" ไม่ใช่ "หัวข้อ" ใครจำเลขเดิมไว้จะได้คนละเรื่อง

    เดิม  ม.1 คณิต No.4  =  การคูณจำนวนเต็ม (ทั้งหัวข้อ)
    ใหม่  ม.1 คณิต No.4  =  การลบจำนวนเต็ม คาบ 2
                 No.9  =  การคูณจำนวนเต็ม คาบ 1

ตรวจเลขใหม่ได้ที่ตารางในไฟล์หลักสูตร หรือหน้า "สั่งทำงาน" บนเว็บซึ่งเลือกจาก
รายการให้แทนการพิมพ์เลขเอง · **วิชาที่ยังไม่มี xlsx รายคาบ (p1-sci · p4-sci ·
p4-english) ไม่เปลี่ยนอะไรเลย** เลข No. และ BASE เหมือนเดิมทุกอย่าง

### BASE มี 4 ท่อน และโฟลเดอร์ซ้อนขึ้นหนึ่งชั้น

    หลักสูตรรายคาบ   M1-Math_U1_4_1
      Output/M1/Math/M1-Math_U1/M1-Math_U1_4/M1-Math_U1_4_1/
    หลักสูตรแบบเดิม   P1-Sci_U1_1        (ไม่เปลี่ยน)
      Output/P1/Sci/P1-Sci_U1/P1-Sci_U1_1/

โฟลเดอร์คาบซ้อนใต้หัวข้อหลัก ไม่วางแบนใต้หน่วย เพราะหน่วยหนึ่งมีได้ถึง 17 คาบ
และโฟลเดอร์ระดับหัวข้อหลักที่ทำไว้ก่อนหน้านี้กลายเป็น "พ่อ" ของคาบพอดี
**งานเดิมไม่ต้องย้ายเลย**

ตารางหลักสูตรรับคอลัมน์ที่ 4 (คาบ) แบบมีก็ได้ไม่มีก็ได้ — ถ้าบังคับใช้ทันที
วิชาที่ยังไม่มี xlsx จะพังหมดทั้งที่งานเดิมถูกต้อง (ทดสอบครบ 4 วิชาแล้ว)

### แบบฝึกหัดค่าเริ่มต้น 20 -> 30 ข้อ

easy 15 / medium 8 / hard 7 · สถานการณ์จริง ≥ 9 ข้อ
`validate_spec.py` ไม่ต้องแก้ — `QUOTA` รองรับ 20/30/40 ตามสัดส่วน 50/25/25 อยู่แล้ว

เกมยังพักอยู่ แต่แก้ด้วย เพราะ `game-checkwork` ตรึง "questions = 20 ข้อ" ไว้เป็นกฎ
ซึ่งจะ FAIL ทันทีที่กลับมาเปิดใช้ — เปลี่ยนเป็นอิงจำนวนข้อในแบบฝึกหัดต้นทางแทน

### `watch.py` — ดูว่างานที่รันอยู่ค้างหรือเปล่า

    eduone-py watch.py m1 math 9

ระหว่างที่ agent คิดหรือรัน tool ยาว ๆ หน้าจอ Claude Code นิ่งเป็นสิบนาที
คนดูแยกไม่ออกว่า "กำลังคิด" กับ "ค้างไปแล้ว" — ตอบด้วยหลักฐาน 2 ชั้น

  ชั้นที่ 1  บันทึกสนทนา `~/.claude/projects/<โฟลเดอร์งาน>/*.jsonl` ขยับทุกครั้งที่
             agent พูดหรือเรียก tool = ชีพจรตัวจริง · เงียบเกิน 5 นาทีเตือน
             เกิน 15 นาทีบอกว่าน่าจะค้าง
  ชั้นที่ 2  ไฟล์ผลผลิตใน `Output/` = คืบหน้าถึงไหน · ชั้นนี้เงียบนานได้ตามปกติ
             (เขียน C1 ทีเดียวใช้เวลา 15-20 นาที)

สองชั้นนี้ตอบคนละคำถาม ถ้าดูแค่ไฟล์ผลผลิตจะเตือนผิดตลอดตอน agent เขียนยาว ๆ
`--json` ไว้ให้ระบบอื่นดึงไปใช้ต่อ

## 1.3.0 — 2026-09-01

**ทุกการเรียก python ผ่านตัวห่อ `eduone-py` ตัวเดียวแล้ว** — แก้ปัญหาสองเรื่องพร้อมกัน

1. **เดิมใช้ได้เฉพาะ Windows** — สกิลเขียน path ของ python ตายตัวเป็น
   `%LOCALAPPDATA%/Programs/Python/Python312/python.exe` อยู่ 69 จุดใน 43 ไฟล์
   พนักงานที่ใช้ macOS พังทั้งระบบตั้งแต่คำสั่งแรก
2. **ตั้งกฎอนุญาตแคบ ๆ ไม่ได้** — กฎของ Claude Code เทียบแบบ "ขึ้นต้นด้วย"
   คำสั่งที่ขึ้นต้นด้วย path เต็มซึ่งต่างกันทุกเครื่องจึงต้องเปิดกว้างทั้งหมด

ตอนนี้เหลือกฎเดียว: `"Bash(eduone-py *)"`

- `bin/eduone-py` (bash) และ `bin/eduone-py.cmd` (Windows) — Claude Code เพิ่ม
  `bin/` ของปลั๊กอินเข้า PATH ให้เอง (ทดสอบยืนยันแล้ว)
- หา Python 3.12 เองตามลำดับ: `EDUONE_PYTHON` → path มาตรฐานของ Windows →
  `python3.12`/`python3`/`python` → `py -3.12` · ตั้ง `PYTHONIOENCODING=utf-8` ให้เสมอ
  (กันภาษาไทยเพี้ยนบน console Windows)
- หาสคริปต์เองจากชื่อไฟล์ — `eduone-py paths.py p4 sci 3` ไม่ต้องรู้ว่าไฟล์อยู่โฟลเดอร์ไหน
- อาร์กิวเมนต์แรกขึ้นต้นด้วย `-` จะส่งต่อให้ python ตรง ๆ (`eduone-py -m json.tool x.json`)
  ทำให้การเรียก python ทั่วไปก็ผ่านตัวห่อได้ ไม่ต้องมีข้อยกเว้น
- หาที่อยู่ตัวเองจากตำแหน่งไฟล์ ไม่พึ่ง env var จึงทำงานได้แม้ `CLAUDE_PLUGIN_ROOT` ไม่ถูกตั้ง
- `install.ps1` เพิ่มขั้นตอนตั้งกฎอนุญาตให้ (ถามก่อน · ไม่แก้ทับถ้ามีไฟล์อยู่แล้ว)

## 1.1.1 — 2026-09-01

- **แก้ path ที่หลงเหลืออีก 33 จุดใน 24 ไฟล์** — ตอนย้ายมาเป็นปลั๊กอินรอบแรก
  แก้เฉพาะ `.claude/skills/shared/` แต่ยังมี `.claude/skills/song/` `.claude/skills/video/`
  `.claude/skills/game/` `.claude/skills/slides/` `.claude/skills/content/personas/`
  เหลืออยู่ ซึ่งจะชี้ไปที่ว่างบนเครื่องพนักงาน (สคริปต์ทำเพลง/วิดีโอ/เกมเรียกไม่ได้)
  ตอนนี้เหลือ `.claude/` แค่ใน _root.py ซึ่งเป็น fallback ของโหมด repo โดยตั้งใจ

## 1.1.0 — 2026-09-01

- เพิ่ม **`install.ps1`** ตัวช่วยติดตั้งสำหรับ Windows — คัดลอกบรรทัดเดียวแล้ว
  มันถามทีละขั้นว่าจะติดตั้งอะไร ไม่ติดตั้งอะไรเองโดยไม่ได้รับคำตอบ · รันซ้ำได้
  ตรวจและติดตั้งให้ได้: Claude Code · Git for Windows · Python 3.12 (ผ่าน winget)
  · ปลั๊กอิน · แพ็กเกจ 11 ตัว · สร้างโฟลเดอร์งาน · ปิดท้ายด้วย doctor.py
- ไฟล์เขียนเป็น UTF-8 **พร้อม BOM** — PowerShell 5.1 ที่มากับ Windows อ่าน UTF-8
  ที่ไม่มี BOM เป็น cp874 ทำให้อักษรไทยเพี้ยนจนสคริปต์พาร์สไม่ผ่าน

## 1.0.2 — 2026-09-01

- **แก้คำสั่งอัปเดตในคู่มือ** — ต้องเป็น `claude plugin update edu-one@eduone`
  ถ้าพิมพ์แค่ `edu-one` จะขึ้น `Plugin "edu-one" not found` (ทดสอบแล้วเจอจริง)
- ระบุให้ชัดว่าต้องปิด-เปิด Claude Code ใหม่หนึ่งครั้งหลังอัปเดต

## 1.0.1 — 2026-09-01

- เพิ่ม `scripts/doctor.py` — ตรวจความพร้อมของเครื่องด้วยคำสั่งเดียว
  บอกทีละข้อว่าอะไรขาดและต้องพิมพ์อะไรแก้ · ใช้ stdlib ล้วนจึงรันได้ก่อนติดตั้งแพ็กเกจ
- **แก้ลำดับขั้นตอนติดตั้งในคู่มือ** — เดิมบอกให้ `pip install -r requirements.txt`
  ก่อนติดตั้งปลั๊กอิน แต่ไฟล์นั้นมากับปลั๊กอิน คนที่ทำตามตรง ๆ จะติดตั้งแต่ขั้นที่ 2
- คู่มือใส่คำสั่งแยกสำหรับ Windows (PowerShell) และ macOS/Linux ที่ทดสอบแล้ว

## 1.0.0 — 2026-09-01

รุ่นแรก — แยก "ความฉลาด" ออกจาก repo งานมาเป็นปลั๊กอิน

- 8 สกิล (`/content` `/lesson-plan` `/exercise` `/slides` `/song` `/video` `/game` `/edu-one`)
- 16 sub-agent (writer + checkwork ครบทุกคู่ · `model: opus` ทั้งหมด)
- สคริปต์ 30 ไฟล์ + ข้อมูลหลักสูตร 4 คู่
- `_root.py` แยกที่อยู่ข้อมูลอ้างอิง (มากับปลั๊กอิน) ออกจากที่วางผลผลิต (ของพนักงาน)
